# Optional: override the port the server listens on (default: 3000)
# PORT=3000

# Optional: override the agents directory (default: ./agents)
# AGENTS_DIR=/path/to/agents

# Optional: override the skills directory (default: ./skills)
# SKILLS_DIR=/path/to/skills
